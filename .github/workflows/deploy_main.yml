name: Deploy Telex Backend - main

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  update_and_restart_app:
    runs-on: telex-server
    steps:
      - name: Change directory to app_be
        run: cd /var/www/telex_be

      - name: Reset git to the latest commit
        run: git reset --hard

      - name: Pull latest changes from the main branch
        run: git pull origin main

      - name: Build the application binary
        run: go build -o telexbe_main

      - name: Restart the application with Supervisor
        run: supervisorctl restart telexbe
          '
